/target
**/*.rs.bk
.env

# dedalo.toml and .dedalo/ are meant to be committed: the funding rules and
# the payout ledger are part of the project's public record.

# forge build output
/src/chain/contract/target


# Build outputs of the release and docs pipelines
/dist
/_site

# Python bytecode from the helper scripts
__pycache__/
*.pyc
