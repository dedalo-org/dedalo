# Every path here is checked by the `main` ruleset, which requires a code
# owner's review. A path that no longer exists silently reviews nothing — the
# `*` line still catches it, but the intent below stops being expressed.
#
# `main` is the 0.0.0 placeholder. The code — and the money paths that were
# guarded here — is on `v0.1`, which carries its own copy of this file. What
# is left below is what `main` actually holds.

# Default: any change needs a maintainer's eyes.
*                                   @4137314

# What is verified, and how. The gate in tests/verification_manifest.rs reads
# this file, so weakening a claim here weakens the build.
/verification.toml                  @4137314
/tests/verification_manifest.rs     @4137314

# Decisions the code is checked against rather than the other way round.
/docs/settlement-architecture.md    @4137314

# Anything that can publish, sign, or ship a binary.
/.github/workflows/                 @4137314
/.github/rulesets/                  @4137314
/action.yml                         @4137314
/install.sh                         @4137314
/SECURITY.md                        @4137314
/Cargo.toml                         @4137314

# The funding rules this project runs on itself. A public record.
/dedalo.toml                        @4137314
/FUNDING.yml                        @4137314
