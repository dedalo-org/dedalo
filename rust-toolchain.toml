# Pinned so every contributor, CI runner and Nix shell builds with the same
# compiler. Bumping this is a reviewable change like any other.
[toolchain]
channel = "1.91.1"
components = ["rustfmt", "clippy", "rust-src", "rust-analyzer"]
profile = "minimal"
