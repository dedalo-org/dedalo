# Pinned so every contributor and CI runner builds with the same compiler.
# rustup reads it on entry to the repository. Bumping this is a reviewable
# change like any other.
[toolchain]
channel = "1.91.1"
components = ["rustfmt", "clippy", "rust-src", "rust-analyzer"]
profile = "minimal"
